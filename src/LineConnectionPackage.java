public class LineConnectionPackage {
    DrawableObject drawObj;
    Drawable.CONNECT_POSITION position;
    public LineConnectionPackage(DrawableObject givenObj, Drawable.CONNECT_POSITION givenPosition) {
        drawObj = givenObj;
        position = givenPosition;
    }
}
